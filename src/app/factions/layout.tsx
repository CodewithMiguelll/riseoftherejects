export const metadata = {
  title: "Factions | Rise of the Rejects",
  description:
    "Discover the factions vying for power in the world of ROTR.",
};

export default function FactionsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <div>{children}</div>;
}
