export const metadata = {
  title: "Lower Faction | Rise of the Rejects",
  description: "Lower Faction in the world of ROTR.",
};

export default function LowersLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <div>{children}</div>;
}
