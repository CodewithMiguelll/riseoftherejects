export const metadata = {
  title: "Reject Faction | Rise of the Rejects",
  description: "Rejects Faction in the world of ROTR.",
};
    
export default function RejectsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <div>{children}</div>;
}
