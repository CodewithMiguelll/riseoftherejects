export const metadata = {
  title: "Upper Faction | Rise of the Rejects",
  description: "Upper Faction in the world of ROTR.",
};

export default function UppersLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <div>{children}</div>;
}
